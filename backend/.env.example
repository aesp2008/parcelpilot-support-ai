# Copy this file to .env (or export these as real environment variables) before running
# the backend. .env itself is gitignored -- never commit a real key.

GEMINI_API_KEY=your-gemini-api-key-here

# Optional overrides -- defaults are set in app/config.py
# GEMINI_MODEL=gemini-3.5-flash-lite
